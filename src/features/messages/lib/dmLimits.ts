/** Alinhado ao servidor (`DirectMessagingService`). */
export const DM_MESSAGE_BODY_MAX_LENGTH = 16_000;

/** Limite de anexos por mensagem na UI (servidor permite até 10). */
export const DM_MESSAGE_MAX_IMAGE_ATTACHMENTS = 6;
