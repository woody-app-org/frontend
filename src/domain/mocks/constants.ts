/**
 * ID da usuária principal do seed (`buildUsers` primeiro item).
 * Usado como fallback quando não há sessão (ex.: testes) e alinhado ao login mock (`auth.service`).
 */
export const MOCK_PRIMARY_USER_ID = "1";
